# AI Gateway (preferred - via Vercel OIDC, no key needed when deployed)
# Or set an API key for local development:
# AI_GATEWAY_API_KEY=your-key-here

# Direct Anthropic API key (fallback if not using AI Gateway)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Database (provision via Vercel Marketplace + Neon)
# DATABASE_URL=postgresql://...
